name: Second GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Create-Second-Job:    
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 ${{ github.actor }}"
      - run: echo "🐧 DEU CERTO A CRIAÇÃO DO SEGUNDO JOB!!!"